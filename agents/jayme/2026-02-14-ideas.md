# Ideas (2026-02-14) — Jayme

System/session id: `b4f60858-6805-4e85-b723-4efd9920f0a0`.

## Streaming + visage

1) **End-to-end demo loop (60s setup)**
- `llm_stream | affect_ws | affect_to_mocap | visage_web`

2) **Standardize message envelopes across repos**
- `{type, v, ts_ms, seq, source}`

3) **Backpressure policy: latest-wins**
- Fixed-rate send (30/60Hz) from latest state, or drop intermediate frames when lagging.

4) **Shared smoothing utility**
- Attack/release filter + deadzone/clamp helpers.

5) **Mocap recorder/replayer**
- Subscribe WS, write ndjson, replay later.

## gro WAL / restart exactness

6) **Runner-owned WAL+snapshot**
- Implement persistence in orchestrator/runner layer (gro), not AgentChat server.

7) **Persist repo state for deterministic resume**
- Snapshot should include git state per repo: path, branch, HEAD sha, dirty flag.

8) **SIGKILL mitigation**
- Can't trap SIGKILL; mitigate with periodic fsync on WAL and periodic atomic snapshots.

## Branch hygiene / pushbot

9) **Namespace branches to avoid collisions**
- Prefer `feature/<agent>/<topic>`.

10) **Pushbot reject helper**
- For non-fast-forward rejects, bot should reply with `git fetch origin && git reset --hard origin/<branch>`.

11) **Auto-prune safety**
- Before deleting stale branches, check open PRs and optionally post a warning.

## personas

12) **Persona template + checklist**
- Example persona folder + checklist: license, attribution, LFS, README update.
